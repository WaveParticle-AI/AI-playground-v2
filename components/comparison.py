"""Display constants shared across comparison columns."""

from __future__ import annotations


THINKING_MODE_BADGES = {
    "default": "default",
    "inner_os": "role-immersion",
    "no_inner_os": "pure-analysis",
}
